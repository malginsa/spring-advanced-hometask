<b:beans xmlns="http://www.springframework.org/schema/security"
         xmlns:b="http://www.springframework.org/schema/beans"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://www.springframework.org/schema/beans
            http://www.springframework.org/schema/beans/spring-beans.xsd
			http://www.springframework.org/schema/security
			http://www.springframework.org/schema/security/spring-security.xsd">

    <!--<http pattern="/index" security="none"/>-->

    <http auto-config="true">
        <intercept-url pattern="/getTickets*" access="hasRole('ROLE_ADMIN')" requires-channel="any"/>
        <intercept-url pattern="/**" access="hasRole('ROLE_USER')" requires-channel="any"/>
        <logout invalidate-session="true"
                logout-success-url="/index"
                delete-cookies="JSESSIONID"/>
        <csrf disabled="true"/>
    </http>

    <authentication-manager>
        <authentication-provider>
            <user-service>
                <user name="1" password="1" authorities="ROLE_USER" />
                <user name="2" password="2" authorities="ROLE_USER, ROLE_ADMIN" />
            </user-service>
        </authentication-provider>
    </authentication-manager>

</b:beans>
